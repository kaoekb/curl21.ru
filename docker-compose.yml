services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: curl21.ru-container
    init: true
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PARROT_PORT: 3000
      TERM: xterm-256color
    restart: unless-stopped
